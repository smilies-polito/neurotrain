name: SHD
supported_net_types: [fc, rec]   # flat 700-channel sequence, no spatial dims
T:
  value: 100
  type: null
  min: 50
  max: 150
  step: 10
pin_memory: true
num_workers: 4
data_root: null   # null = uses SHD_ROOT env var or ../Data/
download: true
use_cache:
  value: true
  type: null   # tonic disk cache; disable only for debugging
