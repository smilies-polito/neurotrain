name: DVSCIFAR10
supported_net_types: [fc, rec, conv, vgg9]
T:
  value: 10
  type: null
  min: 5
  max: 20
  step: 1
pin_memory: false
num_workers: 4
train_fraction: 0.9
data_root: null   # null = uses DVSCIFAR10_ROOT env var or ../Data/
download: true
use_cache:
  value: true
  type: null   # tonic disk cache; disable only for debugging
